---
import Layout from '@/layouts/Layout.astro'
import Banner from '@/components/Banner.astro'

const tiles = [
	{
		name: 'Servers',
		link: 'servers',
		icon: 'fa-solid fa-server'
	},
	{
		name: 'Gaming',
		link: 'gaming',
		icon: 'fa-solid fa-gamepad'
	},
	{
		name: 'Projects',
		link: 'projects',
		icon: 'fa-solid fa-code'
	},
	{
		name: 'Links',
		link: 'links',
		icon: 'fa-solid fa-link'
	}
]
---

<Layout title="henahax.net">
	<div class="flex flex-col items-center justify-center gap-8">
		<Banner isLayout={false} />

		<div class="grid grid-cols-[auto_1fr] gap-4">
			{
				tiles.map((tile) => (
					<a
						class="menu-option menu-item heading col-span-full grid grid-cols-subgrid items-center"
						href={'/' + tile.link}
						transition:name={tile.link + '-card'}
					>
						<div class="col-span-full grid grid-cols-subgrid items-center">
							<i class={tile.icon} transition:name={tile.link + '-icon'} />
							<div transition:name={tile.link + '-title'}>{tile.link}</div>
						</div>
					</a>
				))
			}
		</div>
	</div>
</Layout>
